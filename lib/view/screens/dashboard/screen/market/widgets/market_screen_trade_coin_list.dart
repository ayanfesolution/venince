import 'package:flutter/material.dart';

class MarketScreenTradeCoinList extends StatelessWidget {
  const MarketScreenTradeCoinList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [

      
    ],);
  }
}